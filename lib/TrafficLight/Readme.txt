Cách sử dung:

#include"TrafficLight.h"
TrafficLight trf(0,1);  // Trong đó 0,1 là các chân nối vào mach

Các hàm chính cần biết:
trf.setRedOn() // Bật led đỏ sáng
trf.setYellowOn() // Bật led vàng sáng
trf.setGreenOn()  // Bật led xanh sáng
trf.setOffAll()   // Tắt tất cả led